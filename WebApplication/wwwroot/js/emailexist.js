function checkEmailAvailability(e) { if (!/^[^\s@]+@[^\s@]+\.[^\s@]+$/.test(e)) { $("#errormessage").text("Invalid email format"); return } $.ajax({ url: "/Auth/EmailExist", type: "GET", data: { email: e }, cache: !1, dataType: "json", headers: { "Content-Security-Policy": "default-src 'self'; script-src 'self' https://cdnjs.cloudflare.com/ajax/libs/jquery/3.6.3/jquery.min.js;" }, success: function (e) { $("#errormessage").text(""), "success" == e.result && $("#errormessage").text("Email is already in use") }, error: function () { $("#errormessage").text("Error occurred while checking email availability") } }) }